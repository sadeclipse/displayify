name: Deploy to Remote Server

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
  

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
    
    - name: Setup SSH key
      run: |
        mkdir -p ~/.ssh
        echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/deploy_key
        chmod 600 ~/.ssh/deploy_key
        echo "${{ secrets.REMOTE_HOST }} ssh-rsa AAAAB3..." >> ~/.ssh/known_hosts  # Optional: add host key
    
    - name: Rsync to remote server
      run: |
        rsync -avz -e "ssh -i ~/.ssh/deploy_key -p ${{ secrets.REMOTE_PORT }}" \
          --delete \
          ./ ${{ secrets.REMOTE_USER }}@${{ secrets.REMOTE_HOST }}:/path/to/your/temp/folder/
    
    - name: Execute deployment
      uses: appleboy/ssh-action@v1.0.0
      with:
        host: ${{ secrets.REMOTE_HOST }}
        username: ${{ secrets.REMOTE_USER }}
        key: ${{ secrets.SSH_PRIVATE_KEY }}
        port: ${{ secrets.REMOTE_PORT }}
        script: |
          cd /home/secrets.REMOTE_USER
          
          rm -rf displayify/
          git clone git@github.com:Incept10n/displayify.git
          cd displayify
          
          docker compose build --no-cache
          docker compose down
          docker compose up -d
          
          cd front
          npm install
          npm run build
          cd build
          
          rm -rf /var/www/html/*
          cp -r * /var/www/html/
